describe('Standings Component', () => {
  test('Renders Standings component as expected', () => {
    // const tree = renderer.create(<Standings />).toJSON();
    // expect(tree).toMatchSnapshot();
    console.log('hello World');
  });
});
